@using Tawtheef.Domain.Entities.Users
@model Tawtheef.Notifications.Context.TemplateContext<Tawtheef.Notifications.Templates.FinalizeReviewProfile.FinalizeReviewProfileModel>

@{
    var b = Model.Branding;
    var m = Model.Model;
    var product = b.ProductNameAr;

    var isAccepted = m.Status == UserProfileStatus.Approved;
    var profileUrl = b.RecruitmentUrl;
}

@if (isAccepted)
{
@:تم اعتماد الملف الشخصي
@:
@:تحية طيبة،
@:تم اعتماد ملفكم الشخصي على @product.
@:
@:رابط النظام: @profileUrl
}
else
{
@: تم إرجاع الملف الشخصي للتعديل
@:
@:تحية طيبة،
@:تم إرجاع ملفكم الشخصي للتعديل. يرجى مراجعة البيانات واستكمال التعديلات المطلوبة.
@:
@:رابط النظام: @profileUrl
}
