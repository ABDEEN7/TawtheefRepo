@model Tawtheef.Notifications.Context.TemplateContext<Tawtheef.Notifications.Templates.ChangeJobStatusNeedUpdateNotification.ChangeJobStatusNeedUpdateNotificationModel>
Job Posting Requires Updates

Dear Colleague,

The job posting @(string.IsNullOrWhiteSpace(Model.Model.JobTitle) ? "" : $" \"{Model.Model.JobTitle}\"") requires updates before it can be approved.

Please sign in to @Model.Branding.ProductName, review the requested changes, and resubmit the posting.

Access the portal: @Model.Branding.WebsiteUrl

Regards,
@Model.Branding.ProductName Team
