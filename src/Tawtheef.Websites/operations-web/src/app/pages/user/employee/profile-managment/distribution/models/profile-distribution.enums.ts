export enum EmployeeAvailability {
  Available = 1,
  OnLeave = 2,
  Suspended = 3,
  Inactive = 4,
}

export enum DistributionAssignmentState {
  Assigned = 'Assigned',
  Unassigned = 'Unassigned',
}
