@model Tawtheef.Infrastructure.Services.NotificationServices.TemplateContext<Application.Operation.Templates.ChangeJobStatusApprovedNotification.ChangeJobStatusApprovedNotificationModel>
Job Posting Approved

Dear Colleague,

The job posting "@(string.IsNullOrWhiteSpace(Model.Model.JobTitle) ? "the job posting" : Model.Model.JobTitle)" has been approved.

You may proceed with publishing the role or taking the next steps in @Model.Branding.ProductName.

Access the portal: @Model.Branding.WebsiteUrl

Regards,
@Model.Branding.ProductName Team
Support: @Model.Branding.SupportEmail
